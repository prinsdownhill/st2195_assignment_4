# Function to compute the volumes of the sphere with radius r, r^2 and r^3
volume_vector <- function(r){
r <-22
for (r in 2:4) {
volume(r)
}
}
volume(r)
# Function to compute the volume of A sphere with radius r
volume <- function(r){
3/4*pi*r^2
}
print(volume)
print(pi)
# Function to compute the volume of A sphere with radius r
volume <- function(r, rho){
3/4*pi*r^2
}
print(rho)
print(vol)
vol <- 3/4*pi*r^2
# Function to compute the volume of A sphere with radius r
volume <- function(r){
result <- 3/4*pi*r^2
}
# Function to compute the volumes of the sphere with radius r, r^2 and r^3
volume_vector <- function(r){
r <-22
for (r in 2:4) {
volume(r)
}
}
print(result)
a
# Function to compute the volumes of the sphere with radius r, r^2 and r^3
volume_vector <- function(r){
r <-22
for (r in 2:4) {
volume(r)
}
}
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
volume(r, rho){
# Function to compute the volume of A sphere with radius r
volume <- function(r, rho){
3/4*pi*r^rho
}
3/4*pi*r^rho
}
for (rho in 2:4) {
volume(r, rho){
# Function to compute the volumes of the sphere with radius r, r^2 and r^3
volume_vector <- function(r, rho){
rho <-1
for (rho in 2:4) {
volume(r, rho){
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
volume_vector(r)
# Run volume_vector(r) and print the volumes of the spheres with radius r, r^2 and r^3
print(volume_vector(r))
volume(r, rho){
print(volume(1))
print(volume(1))
print(volume(1))
View(volume)
View(volume)
print(volume(1))
print(volume(1))
# Radius
r <- 2
# Function to compute the volume of A sphere with radius r
volume <- function(r){
3/4*pi*r^1
}
print(volume(r))
print(rho)
for (rho in 2:4) {
volume(r, rho){
print(volume_vector(r, rho))
rho <- 1
}
}
volume(r, rho)
for (rho in 2:4) {
volume(r, rho)
}
# Function to compute the volumes of the sphere with radius r, r^2 and r^3
volume_vector <- function(r, rho){
for (rho in 2:4) {
volume(r, rho)
}
}
print(volume_vector(1,1))
print(volume_vector(r,rho))
View(volume)
# Function to compute the volume of A sphere with radius r
volume <- function(r, rho){
3/4*pi*r^rho
}
print(volume(r, rho))
r <- 2
rho <- 1
# Function to compute the volume of A sphere with radius r
volume <- function(r, rho){
3/4*pi*r^rho
}
print(volume(r, rho))
print(volume(r, rho))
3/4*pi*r^rho
# Function to compute the volume of A sphere with radius r
volume <- function(r, rho){
3/4*pi*r^rho
}
volume_vector <- function(r){
rho <- 1
for (rho in 2:4) {
volume(r, rho){
rho <- 1
volume_vector <- function(r){
for (rho in 2:4) {
volume(r, rho){
volume_vector <- function(r,rho){
for (rho in 2:4) {
volume(r, rho){
volume_vector <- function(r,rho){
for (rho in 2:4) {
volume(r, rho){
volume(r, rho){
for (rho in 2:4) {
volume(r, rho){
volume_vector <- function(r,rho){
for (rho in 2:4) {
r <- r+1
print(rho)
}
}
print(rho)
for (rho in 2:4) {
r <- r+1
print(rho)
}
volume_vector <- function(r,rho){
for (rho in 2:4) {
r <- r+1
print(rho)
}
}
print(rho)
for (rho in 2:4) {
r <- r+1
print(rho)
}
volume_vector <- function(r,rho){
for (rho in 2:4) {
r <- r+1
print(rho)
}
}
volume_vector <- function(r,rho){
for (rho in 2:4) {
r <- r+1
print(rho)
}
}
volume_vector(r,rho)
volume_vector(r,rho)
volume_vector(r,rho)
volume_vector(r,rho)
volume_vector(r,rho)
print(volume(r,rho))
print(volume(r,rho))
for (rho in 2:4) {
rho <- rho+1
print(volume(r,rho))
}
volume_vector <- function(r,rho){
for (rho in 2:4) {
rho <- rho+1
print(volume(r,rho))
}
}
print(volume(r, rho))
volume_vector(r,rho)
